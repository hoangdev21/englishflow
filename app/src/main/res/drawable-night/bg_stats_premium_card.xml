<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow / Base Depth Layer -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="@color/ef_silver_shadow" />
        </shape>
    </item>

    <!-- Main Card Body -->
    <item android:bottom="4dp">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <stroke android:width="1.5dp" android:color="@color/ef_silver_border" />
            <gradient
                android:angle="270"
                android:centerY="0.2"
                android:endColor="@color/ef_white_silver"
                android:startColor="#1E2D2A"
                android:type="linear" />
        </shape>
    </item>

    <!-- Top Highlight (Subtle Shine) -->
    <item android:bottom="5dp" android:top="2dp" android:left="2dp" android:right="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <gradient
                android:angle="90"
                android:endColor="#05FFFFFF"
                android:startColor="@android:color/transparent"
                android:type="linear" />
        </shape>
    </item>
</layer-list>
